<entry name="{{ admin_name }}">
    <permissions>
        <role-based>
            <custom>
                <profile>{{ role_name }}</profile>
            </custom>
        </role-based>
    </permissions>
    <phash>{{ admin_password | md5_hash }}</phash>
</entry>